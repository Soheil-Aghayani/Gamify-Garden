# نقشه ساخت Gamify Garden

## هدف

ساخت یک وب‌اپ شخصی، فارسی، راست‌به‌چپ و موبایل‌محور با حال‌وهوای «باغ جادویی گوگولی» برای انجام قدم‌های کوچک روزانه.

## تصمیم‌های محصول

- نسخه اول یک بازی شخصی و بدون لاگین است.
- پیشرفت در `localStorage` مرورگر ذخیره می‌شود.
- نام پیش‌فرض فاطمه و نام صمیمی باغ `Apricity` است؛ Apricity یعنی گرمای نور خورشید در زمستان.
- هر روز کاربر تا سه مأموریت را از فهرست شخصی خود انجام می‌دهد؛ اگر تعداد مأموریت‌ها کمتر باشد، هدف روز به همان تعداد کاهش پیدا می‌کند.
- شش مأموریت اولیه شامل LLM، ویدئوی مقاله‌نویسی، زبان، مقاله، ورزش و نوشتن هستند.
- کاربر می‌تواند مأموریت‌های تازه با عنوان، قدم حداقلی و آیکون بسازد یا مأموریت‌های موجود را حذف کند.
- هر مأموریت نسخه حداقلی دارد تا شروع‌کردن سخت نباشد.
- سه مأموریت کامل‌شده یک برد روزانه و یک شکوفه ایجاد می‌کند.
- امتیاز منفی وجود ندارد و استریک فقط به‌صورت ملایم نمایش داده می‌شود.
- انرژی روز در سه حالت آرام، معمولی و پرانرژی انتخاب می‌شود.
- راهنمای درون‌برنامه‌ای، منطق باغ و روش استفاده از انرژی، قدم‌ها و تسک‌ها را کوتاه توضیح می‌دهد.
- flow نیمه‌هدایت‌شده است: راهنما فقط بار اول باز می‌شود و بعد از انرژی، مأموریت، رشد و جایزه، تمرکز به قدم بعدی منتقل می‌شود.
- آواتار روزانه با `boring-avatars` به‌صورت SVG محلی ساخته می‌شود؛ seed و variant هر روز به‌شکل deterministic تغییر می‌کنند و در هدر و صحنه‌ی رشد دیده می‌شوند.
- تاریخ امروز با تقویم شمسی، نام روز هفته و آواتار همان روز در هدر نمایش داده می‌شود.
- یادگارهای هفته فقط شکوفه‌های روزهای موفق را نشان می‌دهند و هیچ روزی را جریمه نمی‌کنند.
- کتابخانه‌ی جایزه‌ها متنوع است، هر روز چند پیشنهاد متفاوت نشان می‌دهد و جایزه‌های شخصی نیز قابل اضافه‌کردن هستند.
- برنامه به‌شکل PWA با حالت standalone، install prompt، آیکون‌های نصب، safe area موبایل و cache آفلاین اجرا می‌شود.

## تصمیم‌های تکمیلی باغ قابل‌کاشت — ۱۵ اوت ۲۰۲۶

برای نزدیک‌شدن تجربه‌ی باغ به بازی‌های cozy مثل Straw/Sprout Valley، خود باغ به‌عنوان یک Board مستقل رفتار می‌کند:

- بالای کارت باغ یک «نوار کاشت» یا hotbar ثابت و قابل‌دسترس وجود دارد؛ دانه‌ها نباید فقط پایین صفحه در دسترس باشند.
- کاربر ابتدا از نوار کاشت یکی از دانه‌های آزاد را لمس می‌کند، سپس جایگاه خالی باغ را می‌زند؛ جایگاه‌های مناسب با حاشیه و pulse نرم برجسته می‌شوند.
- نوار کاشت وضعیت هر دانه را نشان می‌دهد: درخت آماده‌ی امروز، گل آزادشده و بوته‌ی قفل‌شده یا آزادشده. دانه‌های قفل‌شده فقط توضیح کوتاه آزادشدن را نشان می‌دهند.
- بعد از انتخاب دانه، متن نوار به «حالا یک جای خالی را لمس کن» تغییر می‌کند و در Board نیز یک راهنمای کوچک دیده می‌شود.
- سینی دانه‌ی تکراری از پایین Board حذف شد تا یک منبع اصلی و واضح برای انتخاب دانه وجود داشته باشد؛ انتخاب از بالای باغ انجام می‌شود و کاشت روی جایگاه صورت می‌گیرد.
- نوار کاشت با ظاهر شیشه‌ای، گوشه‌های گرد، رنگ‌های پاستلی و `backdrop-filter` طراحی شده تا حس نوار ابزار بازی داشته باشد و هنگام مرور باغ در دسترس بماند.
- دانه‌ی درخت همچنان فقط بعد از برد روزانه قابل استفاده است؛ «بعداً می‌کارمش» دانه را حذف نمی‌کند و کاربر بعداً می‌تواند از همان نوار آن را انتخاب کند.
- تغییرات این بخش در `src/components/GardenDecor.tsx` و `src/styles.css` پیاده‌سازی شده و منطق انتخاب/کاشت قبلی در `src/lib/game.ts` حفظ می‌شود.
- بعد از هر تغییر در Board، `npm run typecheck`، `npm test` و `npm run build` باید اجرا شوند و رفتار در عرض‌های ۳۲۰، ۳۹۰ و ۴۳۰ پیکسل بررسی شود.

## بازطراحی بصری باغ به سبک مزرعه‌ی top-down — ۱۵ اوت ۲۰۲۶

مرجع جدید نشان داد که هدف صرفاً یک کارت باغ پاستیلی نیست؛ باغ باید حس یک نقشه‌ی کوچک مزرعه‌ای شبیه Stardew Valley داشته باشد، بدون کپی‌کردن asset یا تصویر بازی:

- صحنه‌ی قدیمیِ تپه‌ای و مینیمال حذف می‌شود و به یک نقشه‌ی top-down با نسبت عریض تبدیل می‌شود.
- نقشه شامل چمن tile-like، زمین‌های قهوه‌ای قابل‌کاشت، مسیر خاکی، خانه‌ی چوبی، چاه، نیمکت، صندوق پست، کنده، درخت، بوته، گل و حصار است.
- جایگاه‌های واقعی `plot-1` تا `plot-12` داخل قطعه‌ی خاک نقشه قرار می‌گیرند؛ جایگاه‌ها دیگر کارت‌های جدا از جهان بازی نیستند.
- جایگاه خالی با `+`، جایگاه قفل با قفل، جایگاه انتخاب‌شده با outline طلایی و جایگاه مناسب کاشت با pulse سبز/زرد دیده می‌شود.
- آیتم‌های کاشته‌شده با `PixelPlantArtwork` به شکل درخت، گل و بوته‌ی pixel-style روی نقشه نمایش داده می‌شوند و وضعیت اصلی `PlantedGardenItem` را مصرف می‌کنند.
- وقتی دانه‌ی درخت آماده باشد، یک پیش‌نمایش کم‌رنگ روی نقشه نمایش داده می‌شود؛ این پیش‌نمایش جایگاه واقعی را مصرف نمی‌کند و فقط با انتخاب جایگاه به آیتم کاشته‌شده تبدیل می‌شود.
- نوار کاشت بالای نقشه باقی می‌ماند و نقش inventory/hotbar را دارد: انتخاب دانه از بالا، سپس انتخاب زمین از روی نقشه.
- کامپوننت‌های اصلی این بازطراحی `src/components/PixelGardenMap.tsx` و `src/components/PixelPlantArtwork.tsx` هستند؛ منطق کاشت در `GardenDecor` و `src/lib/game.ts` تغییر نمی‌کند.
- در dark mode رنگ‌های زمین، چمن، خاک و خانه به نسخه‌ی شبانه‌ی هماهنگ تبدیل می‌شوند و در موبایل نقشه‌ی عریض به قاب بلندتر و قابل لمس تغییر می‌کند.
- هدف این مرحله «حس مزرعه‌ی قابل بازی» است: نقشه باید در نگاه اول از یک dashboard یا کارت تزئینی قابل تشخیص باشد.

## پالایش بصری نقشه‌ی مزرعه — ۱۵ اوت ۲۰۲۶

در بازبینی رفرنس، مشخص شد که صرفاً اضافه‌کردن آبجکت‌های pixel-style کافی نیست و اگر ناحیه‌ها روی هم بیفتند، باغ شلوغ و ناهماهنگ دیده می‌شود. برای همین ترکیب‌بندی نقشه یک‌بار دیگر پالایش شد:

- خانه و حیاط در بخش بالایی نقشه، مسیر اصلی در میانه و قطعه‌ی قابل‌کاشت در بخش پایینی قرار گرفتند.
- زمین اصلی به یک قطعه‌ی خاکی منظم با ۶ ستون و ۲ ردیف جایگاه تبدیل شد؛ جایگاه‌ها با فاصله‌ی یکسان درون خاک قرار دارند.
- باغچه‌های کوچک کناری برای محصول‌های تزئینی، حاشیه‌ی درخت و بوته برای قاب‌کردن نقشه، و چاه/نیمکت در فضای خالی سمت راست قرار گرفتند.
- جزئیات ریز و تزئینات روی مسیر و زمین اصلی کم شدند تا آبجکت‌های تعاملی واضح‌تر باشند.
- در موبایل، قاب نقشه بلندتر می‌شود و جایگاه‌ها اندازه‌ی لمس مناسب‌تری می‌گیرند؛ dark mode همان سلسله‌مراتب را با کنتراست شبانه حفظ می‌کند.
- این مرحله با کلاس `pixel-garden-map--cozy` روی همان منطق قبلی انجام شده تا رفتار کاشت، جابه‌جایی، حذف و Undo تغییر نکند.

## هویت بصری

- پالت پاستلی کرم، سبز نعنایی، یاسی، هلویی و زرد ملایم.
- جوانه‌ای که با پیشرفت از دانه به جوانه، گل و درخت تبدیل می‌شود.
- کارت‌های بزرگ، آیکون‌های واضح، متن کم و انیمیشن‌های کوتاه.
- استفاده از فونت خوانای `Vazirmatn` برای متن فارسی و رابط راست‌به‌چپ.
- استفاده از SVG و CSS؛ بدون تصاویر سنگین یا API خارجی.

## معماری

- React، Vite، TypeScript و `lucide-react`.
- صفحه اصلی بدون Router در نسخه اول.
- داده‌ها، از جمله تسک‌های شخصی‌سازی‌شده، در کلید `gamify-garden:v1` ذخیره می‌شوند.
- وضعیت flow، seed آواتار و جایزه‌های شخصی‌سازی‌شده نیز در همان کلید ذخیره و هنگام migration با مقدار امن کامل می‌شوند.
- کامپوننت‌های راهنما و مدیریت تسک، کنار حلقه‌ی اصلی باغ قرار دارند تا کاربر در هر زمان بتواند مسیرش را تنظیم کند.
- انتشار خودکار با GitHub Actions روی GitHub Pages آماده شده است.
- manifest، service worker و آیکون‌های ۱۹۲ و ۵۱۲ برای نصب PWA در مسیر `public/` قرار دارند.
- رفتارهای مزاحم موبایل مثل pull-to-refresh، انتخاب تصادفی متن و context menu داخل خود باغ محدود شده‌اند؛ zoom و ورودی‌های متنی برای دسترس‌پذیری باز می‌مانند.

## معیار پذیرش

- صفحه در عرض ۳۲۰ تا ۴۳۰ پیکسل بدون اسکرول افقی درست کار کند.
- کاربر در کمتر از ده ثانیه بفهمد باید روی کارت‌ها بزند.
- با انتخاب سه مأموریت، حلقه پیشرفت و گیاه رشد کنند.
- refresh پیشرفت روز را حفظ کند.
- مأموریت‌ها قابل برگشت باشند.
- راهنما از صفحه اصلی قابل دسترسی باشد.
- تسک‌ها بدون حساب کاربری اضافه و حذف شوند و بعد از refresh باقی بمانند.
- آواتار در طول یک روز با refresh ثابت بماند و در روز بعد تغییر کند.
- تاریخ شمسی و روز هفته‌ی امروز در هدر دیده شوند.
- روی موبایل، manifest و service worker امکان نصب standalone و بازشدن آفلاین را فراهم کنند.
- کاربر بتواند جایزه‌ی شخصی اضافه کند و در پایان روز پیام آرام دریافت کند.
- نسخه‌ی build شده manifest و service worker را شامل شود.
- نام و پالت رنگ از تنظیمات قابل تغییر باشند.
- `npm run build` و `npm test` بدون خطا اجرا شوند.
- انیمیشن‌ها به `prefers-reduced-motion` احترام بگذارند.

## خارج از محدوده نسخه اول

لاگین، همگام‌سازی بین دستگاه‌ها، نوتیفیکیشن، اپ native، صدا، چت، پرداخت و تولید مأموریت با هوش مصنوعی.
